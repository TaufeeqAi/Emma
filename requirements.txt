#Vector Store
qdrant-client

# Embeddings + Reranking
sentence-transformers
scikit-learn

# LangChain + langgraph
langchain
langchain-community
langchain-groq
langgraph

#LLM
groq

# ── Safety
nemoguardrails

# ── Voice: STT / TTS / VAD / Audio 
kokoro
soundfile
webrtcvad
pyaudio

# ── Web Framework 
fastapi
uvicorn[standard]
websockets
python-multipart

# ── Observability 
langfuse==2.57.4

# ── Evaluation 
ragas        
datasets    
deepeval       
openai       

# ── LiveKit Telephony 
# Core LiveKit Python SDK
livekit
# Agents framework (worker, JobContext, AutoSubscribe)
livekit-agents
# Server-side API (SIP trunks, dispatch rules, room management, webhooks)
livekit-api
# Silero VAD plugin (lightweight, CPU-only, ~2ms/frame at 16kHz)
livekit-plugins-silero
# Protocol buffers (SIP messages, webhook event models)
livekit-protocol

# Utilities
python-dotenv
pydantic
numpy
pydantic
aiofiles
tabulate
cryptography

# testing
pytest
pytest-asyncio
httpx
pytest-mock